# invoice_extractor

## 이 저장소는 개발이 멈췄다

마지막 작업은 2026-08-20 의 `v0.2.3` (`228d50d`) 이다. 기능은 다른 곳으로 이관됐고,
여기에는 기능 추가·버그 수정·릴리스 계획이 없다. 자세한 건 `README.md` 맨 위 공지를 볼 것.

**받아서 그냥 돌리면 동작하지 않는 게 정상이다.** 로그인된 Claude Code CLI, 구글시트
서비스 계정 키, 초기화된 시트가 모두 살아 있어야 하고, 의존성은 고정돼 있지 않으며 중단
이후 검증된 적이 없다. `node_modules` 도 저장소에 없다.

그래서 실행·빌드·의존성 설치(`npm run dev`, `npm install`, `cargo build`, `tauri build` …)를
하려고 하면 `.claude/hooks/stopped-project-guard.sh` 훅이 먼저 사용자에게 확인을 받는다.
훅이 걸리면 우회하지 말고, 멈춘 저장소라는 사실을 사용자에게 알린 뒤 정말 진행할지 물어볼 것.

읽기·조사(코드 읽기, `git log`, 검색)는 제약 없이 해도 된다.
